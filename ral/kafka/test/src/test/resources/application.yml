spring:
  kafka:
    bootstrap-servers: http://localhost:9092
    consumer:
      bootstrap-servers: http://localhost:9092
    producer:
      bootstrap-servers: http://localhost:9092
jima:
  test-container:
    kafka:
      image: "confluentinc/cp-kafka:7.5.5"
