spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      bootstrap-servers: localhost:9092
    producer:
      bootstrap-servers: localhost:9092
jima:
  test-container:
    kafka:
      image: "apache/kafka:latest"
