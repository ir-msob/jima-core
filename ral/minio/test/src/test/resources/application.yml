spring:
  minio:
    url: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin
    bucket: test
jima:
  test-container:
    minio:
      image: "minio/minio:RELEASE.2024-07-10T18-41-49Z.fips"
      access-key: "minioadmin"
      secret-key: "minioadmin"
