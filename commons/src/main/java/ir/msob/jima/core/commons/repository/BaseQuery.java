package ir.msob.jima.core.commons.repository;

/**
 * Marker interface representing a query definition within the application.
 * <p>
 * This interface does not declare any methods. Its primary purpose is to serve as a common
 * type for all query objects used throughout the system, providing type safety and
 * enabling consistent handling of different query types.
 * <p>
 * Implement this interface in any class that encapsulates query parameters or logic
 * for data retrieval or filtering.
 */
public interface BaseQuery {
}
